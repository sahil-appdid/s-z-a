# To download the dependencies run the commands

# composer install 
# npm install

# To run the project run the commands

# npm run watch 
# php artisan serve --host=0.0.0.0
# php artisan queue:work 

# setup the mail by adding the credentials in .env file
# setup the database using migration